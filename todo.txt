Normal flow
- time out (150ms - 300ms)
- if (2 nodes time out at the same time) -> time out (150ms - 300ms)
- leader = if (candidate votes > (total_nodes / 2))
- leader send a blank message to all the notes (dominance)
- leader send log to repliacte in chunks

if (Leader dead)
- if (followers dont receive blank message) -> time out (150ms - 300ms)
- if (time out == 0 && replies == 0) -> (self vote + request vote) ? if (other node > current node && node log index > current log index) -> grant vote ? denied vote

if (node dead)
- if (node live) -> (update term) + if (leader find out node log index is low) -> leader send log to replicate (node log index history to leader log index history)
					
